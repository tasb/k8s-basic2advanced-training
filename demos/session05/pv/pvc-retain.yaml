apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sample-pv-claim-retain
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: microk8s-hostpath-retain
  resources:
    requests:
      storage: 10Mi